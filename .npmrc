# Allow React 19 to satisfy packages that declare react@^18 peer deps.
# React 19 is backward-compatible; this is the correct way to handle
# the ecosystem lag rather than downgrading React.
legacy-peer-deps=true
